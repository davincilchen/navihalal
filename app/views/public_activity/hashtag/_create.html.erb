<% if activity.trackable %>
  tag
  <%= link_to activity.trackable.restaurant.name, activity.trackable.restaurant%>
  <%= link_to activity.trackable.tag.name, activity.trackable.tag%>
<% else %>
  hashtag has been removed.
<% end %>