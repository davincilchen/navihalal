<br>
<div class="container">
  <h4><span class="badge badge-pill badge-success"><%=@tag.name%></span></h4>
  <br>
  <div class="row">
      <% @restaurants.each do |restaurant| %>
        <div class="col-sm-6 col-md-4">
          <div class="restaurant-item">
            <div class="restaurant-image">
              <% if restaurant.photo? %>
                 <%= image_tag restaurant.photo.url, class: "img-responsive center-block" %>
              <% else %>
                 <span class="glyphicon glyphicon-picture"></span>
              <% end %>
            </div>
            <br>
            <div class="caption">
              <h4><%= link_to restaurant.name, restaurant_path(restaurant) %></h4>
              <p><%= truncate(restaurant.intro, length: 140) %></p>
            </div>
          </div>
        </div>
      <% end %>
  </div>
</div>
